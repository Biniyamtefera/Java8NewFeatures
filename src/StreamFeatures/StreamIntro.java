package StreamFeatures;

import java.util.stream.Stream;

public class StreamIntro {
    public static void main(String[] args) {
//        Stream.of("Biniyam","Tefera","Amdebrhan").map(s -> s.toUpperCase())
//                .filter(n -> n.length()==5)
//                .sorted()
//                .forEach(System::out.print);
        /**
         * Stream can start
         * Intermediate stream
         * Terminate stream
         * ==> Filetr is more used in intermidate
         * ==> one format to another Employee=Name
         * ==> Sort=Uniqe
         * --Terminal operation
         * toSet
         * toList
         * allMatch/anyMatch
         * noneMatch
         * FirstMatch
         */

    }
}
